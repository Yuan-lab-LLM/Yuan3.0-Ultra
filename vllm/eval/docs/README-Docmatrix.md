# Docmatrix
## 1. Modify and run the scoring bash script
You only need to modify the contents in the bash file, including the paths and table names.
--path  result file

Run the bash script after making the modifications：
```bash Docmatrix_Cauldron/eval_mrag.sh```

## 2. View scoring results
The result will be displayed directly on the screen.